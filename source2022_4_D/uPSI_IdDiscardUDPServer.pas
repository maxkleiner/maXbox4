unit uPSI_IdDiscardUDPServer;
{
This file has been generated by UnitParser v0.7, written by M. Knight
and updated by NP. v/d Spek and George Birbilis. 
Source Code from Carlo Kok has been used to implement various sections of
UnitParser. Components of ROPS are used in the construction of UnitParser,
code implementing the class wrapper is taken from Carlo Kok's conv utility

}
interface
 
uses
   SysUtils
  ,Classes
  ,uPSComponent
  ,uPSRuntime
  ,uPSCompiler
  ;
 
type 
(*----------------------------------------------------------------------------*)
  TPSImport_IdDiscardUDPServer = class(TPSPlugin)
  protected
    procedure CompileImport1(CompExec: TPSScript); override;
    procedure ExecImport1(CompExec: TPSScript; const ri: TPSRuntimeClassImporter); override;
  end;
 
 
{ compile-time registration functions }
procedure SIRegister_TIdDiscardUDPServer(CL: TPSPascalCompiler);
procedure SIRegister_IdDiscardUDPServer(CL: TPSPascalCompiler);

{ run-time registration functions }
procedure RIRegister_TIdDiscardUDPServer(CL: TPSRuntimeClassImporter);
procedure RIRegister_IdDiscardUDPServer(CL: TPSRuntimeClassImporter);

procedure Register;

implementation


uses
   IdAssignedNumbers
  ,IdSocketHandle
  ,IdUDPBase
  ,IdUDPServer
  ,IdDiscardUDPServer
  ;
 
 
procedure Register;
begin
  RegisterComponents('Pascal Script', [TPSImport_IdDiscardUDPServer]);
end;

(* === compile-time registration functions === *)
(*----------------------------------------------------------------------------*)
procedure SIRegister_TIdDiscardUDPServer(CL: TPSPascalCompiler);
begin
  //with RegClassS(CL,'TIdUDPServer', 'TIdDiscardUDPServer') do
  with CL.AddClassN(CL.FindClass('TIdUDPServer'),'TIdDiscardUDPServer') do
  begin
    RegisterMethod('Constructor Create( AOwner : TComponent)');
  end;
end;

(*----------------------------------------------------------------------------*)
procedure SIRegister_IdDiscardUDPServer(CL: TPSPascalCompiler);
begin
  SIRegister_TIdDiscardUDPServer(CL);
end;

(* === run-time registration functions === *)
(*----------------------------------------------------------------------------*)
procedure RIRegister_TIdDiscardUDPServer(CL: TPSRuntimeClassImporter);
begin
  with CL.Add(TIdDiscardUDPServer) do
  begin
    RegisterConstructor(@TIdDiscardUDPServer.Create, 'Create');
  end;
end;

(*----------------------------------------------------------------------------*)
procedure RIRegister_IdDiscardUDPServer(CL: TPSRuntimeClassImporter);
begin
  RIRegister_TIdDiscardUDPServer(CL);
end;

 
 
{ TPSImport_IdDiscardUDPServer }
(*----------------------------------------------------------------------------*)
procedure TPSImport_IdDiscardUDPServer.CompileImport1(CompExec: TPSScript);
begin
  SIRegister_IdDiscardUDPServer(CompExec.Comp);
end;
(*----------------------------------------------------------------------------*)
procedure TPSImport_IdDiscardUDPServer.ExecImport1(CompExec: TPSScript; const ri: TPSRuntimeClassImporter);
begin
  RIRegister_IdDiscardUDPServer(ri);
end;
(*----------------------------------------------------------------------------*)
 
 
end.
