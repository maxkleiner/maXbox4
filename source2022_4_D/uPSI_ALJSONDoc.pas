unit uPSI_ALJSONDoc;
{
This file has been generated by UnitParser v0.7, written by M. Knight
and updated by NP. v/d Spek and George Birbilis. 
Source Code from Carlo Kok has been used to implement various sections of
UnitParser. Components of ROPS are used in the construction of UnitParser,
code implementing the class wrapper is taken from Carlo Kok's conv utility

}
interface
 
uses
   SysUtils
  ,Classes
  ,uPSComponent
  ,uPSRuntime
  ,uPSCompiler
  ;
 
type 
(*----------------------------------------------------------------------------*)
  TPSImport_ALJSONDoc = class(TPSPlugin)
  protected
    procedure CompileImport1(CompExec: TPSScript); override;
    procedure ExecImport1(CompExec: TPSScript; const ri: TPSRuntimeClassImporter); override;
  end;
 
 
{ compile-time registration functions }
procedure SIRegister_ALJSONDoc(CL: TPSPascalCompiler);

{ run-time registration functions }
procedure RIRegister_ALJSONDoc_Routines(S: TPSExec);

procedure Register;

implementation


uses
   ALStringList
  ,ALJSONDoc
  ;
 
 
procedure Register;
begin
  RegisterComponents('Pascal Script', [TPSImport_ALJSONDoc]);
end;

(* === compile-time registration functions === *)
(*----------------------------------------------------------------------------*)
procedure SIRegister_ALJSONDoc(CL: TPSPascalCompiler);
begin
 CL.AddDelphiFunction('Procedure ALJSONToTStrings( const AJsonStr : AnsiString; aLst : TALStrings; const aNullStr : AnsiString; const aTrueStr : AnsiString; const aFalseStr : AnsiString)');
end;

(* === run-time registration functions === *)
(*----------------------------------------------------------------------------*)
procedure RIRegister_ALJSONDoc_Routines(S: TPSExec);
begin
 //S.RegisterDelphiFunction(@ALJSONToTStrings, 'ALJSONToTStrings', cdRegister);
end;

 
 
{ TPSImport_ALJSONDoc }
(*----------------------------------------------------------------------------*)
procedure TPSImport_ALJSONDoc.CompileImport1(CompExec: TPSScript);
begin
  SIRegister_ALJSONDoc(CompExec.Comp);
end;
(*----------------------------------------------------------------------------*)
procedure TPSImport_ALJSONDoc.ExecImport1(CompExec: TPSScript; const ri: TPSRuntimeClassImporter);
begin
  //RIRegister_ALJSONDoc(ri);
  RIRegister_ALJSONDoc_Routines(CompExec.Exec); // comment it if no routines
end;
(*----------------------------------------------------------------------------*)
 
 
end.
