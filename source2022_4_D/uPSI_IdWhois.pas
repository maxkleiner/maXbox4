unit uPSI_IdWhois;
{
This file has been generated by UnitParser v0.7, written by M. Knight
and updated by NP. v/d Spek and George Birbilis. 
Source Code from Carlo Kok has been used to implement various sections of
UnitParser. Components of ROPS are used in the construction of UnitParser,
code implementing the class wrapper is taken from Carlo Kok's conv utility

}
interface
 
uses
   SysUtils
  ,Classes
  ,uPSComponent
  ,uPSRuntime
  ,uPSCompiler
  ;
 
type 
(*----------------------------------------------------------------------------*)
  TPSImport_IdWhois = class(TPSPlugin)
  protected
    procedure CompileImport1(CompExec: TPSScript); override;
    procedure ExecImport1(CompExec: TPSScript; const ri: TPSRuntimeClassImporter); override;
  end;
 
 
{ compile-time registration functions }
procedure SIRegister_TIdWhois(CL: TPSPascalCompiler);
procedure SIRegister_IdWhois(CL: TPSPascalCompiler);

{ run-time registration functions }
procedure RIRegister_TIdWhois(CL: TPSRuntimeClassImporter);
procedure RIRegister_IdWhois(CL: TPSRuntimeClassImporter);

procedure Register;

implementation


uses
   IdAssignedNumbers
  ,IdTCPClient
  ,IdWhois
  ;
 
 
procedure Register;
begin
  RegisterComponents('Pascal Script', [TPSImport_IdWhois]);
end;

(* === compile-time registration functions === *)
(*----------------------------------------------------------------------------*)
procedure SIRegister_TIdWhois(CL: TPSPascalCompiler);
begin
  //with RegClassS(CL,'TIdTCPClient', 'TIdWhois') do
  with CL.AddClassN(CL.FindClass('TIdTCPClient'),'TIdWhois') do
  begin
    RegisterMethod('Constructor Create( AOwner : TComponent)');
    RegisterMethod('Function WhoIs( const ADomain : string) : string');
  end;
end;

(*----------------------------------------------------------------------------*)
procedure SIRegister_IdWhois(CL: TPSPascalCompiler);
begin
  SIRegister_TIdWhois(CL);
end;

(* === run-time registration functions === *)
(*----------------------------------------------------------------------------*)
procedure RIRegister_TIdWhois(CL: TPSRuntimeClassImporter);
begin
  with CL.Add(TIdWhois) do
  begin
    RegisterConstructor(@TIdWhois.Create, 'Create');
    RegisterMethod(@TIdWhois.WhoIs, 'WhoIs');
  end;
end;

(*----------------------------------------------------------------------------*)
procedure RIRegister_IdWhois(CL: TPSRuntimeClassImporter);
begin
  RIRegister_TIdWhois(CL);
end;

 
 
{ TPSImport_IdWhois }
(*----------------------------------------------------------------------------*)
procedure TPSImport_IdWhois.CompileImport1(CompExec: TPSScript);
begin
  SIRegister_IdWhois(CompExec.Comp);
end;
(*----------------------------------------------------------------------------*)
procedure TPSImport_IdWhois.ExecImport1(CompExec: TPSScript; const ri: TPSRuntimeClassImporter);
begin
  RIRegister_IdWhois(ri);
end;
(*----------------------------------------------------------------------------*)
 
 
end.
