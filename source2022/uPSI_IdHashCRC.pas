unit uPSI_IdHashCRC;
{
This file has been generated by UnitParser v0.7, written by M. Knight
      test with computefilecrc32
}
interface
 
uses
   SysUtils
  ,Classes
  ,uPSComponent
  ,uPSRuntime
  ,uPSCompiler
  ;
 
type 
(*----------------------------------------------------------------------------*)
  TPSImport_IdHashCRC = class(TPSPlugin)
  protected
    procedure CompileImport1(CompExec: TPSScript); override;
    procedure ExecImport1(CompExec: TPSScript; const ri: TPSRuntimeClassImporter); override;
  end;
 
 
{ compile-time registration functions }
procedure SIRegister_TIdHashCRC32(CL: TPSPascalCompiler);
procedure SIRegister_TIdHashCRC16(CL: TPSPascalCompiler);
procedure SIRegister_IdHashCRC(CL: TPSPascalCompiler);

{ run-time registration functions }
procedure RIRegister_TIdHashCRC32(CL: TPSRuntimeClassImporter);
procedure RIRegister_TIdHashCRC16(CL: TPSRuntimeClassImporter);
procedure RIRegister_IdHashCRC(CL: TPSRuntimeClassImporter);

procedure Register;

implementation


uses
   IdHash
  ,IdHashCRC
  ;
 
 
procedure Register;
begin
  RegisterComponents('Pascal Script', [TPSImport_IdHashCRC]);
end;

(* === compile-time registration functions === *)
(*----------------------------------------------------------------------------*)
procedure SIRegister_TIdHashCRC32(CL: TPSPascalCompiler);
begin
  //with RegClassS(CL,'TIdHash32', 'TIdHashCRC32') do
  with CL.AddClassN(CL.FindClass('TIdHash32'),'TIdHashCRC32') do begin
    RegisterMethod('Constructor Create;');
    RegisterMethod('Function HashValue(AStream: TStream): LongWord');
    //RegisterMethod('Procedure Free;');
  end;
end;

(*----------------------------------------------------------------------------*)
procedure SIRegister_TIdHashCRC16(CL: TPSPascalCompiler);
begin
  //with RegClassS(CL,'TIdHash16', 'TIdHashCRC16') do
  with CL.AddClassN(CL.FindClass('TIdHash16'),'TIdHashCRC16') do begin
  RegisterMethod('Constructor Create;');
    RegisterMethod('Function HashValue(AStream: TStream): Word');
    //RegisterMethod('Procedure Free;');
  end;
end;

(*----------------------------------------------------------------------------*)
procedure SIRegister_IdHashCRC(CL: TPSPascalCompiler);
begin
  SIRegister_TIdHashCRC16(CL);
  SIRegister_TIdHashCRC32(CL);
end;

(* === run-time registration functions === *)
(*----------------------------------------------------------------------------*)
procedure RIRegister_TIdHashCRC32(CL: TPSRuntimeClassImporter);
begin
  with CL.Add(TIdHashCRC32) do begin
  RegisterConstructor(@TIdHashCRC32.Create, 'Create');
    RegisterMethod(@TIdHashCRC32.HashValue, 'HashValue');
    //RegisterMethod(@TIdHashCRC32.Free,'Procedure Free;');
  end;
end;

(*----------------------------------------------------------------------------*)
procedure RIRegister_TIdHashCRC16(CL: TPSRuntimeClassImporter);
begin
  with CL.Add(TIdHashCRC16) do begin
  RegisterConstructor(@TIdHashCRC16.Create, 'Create');
    RegisterMethod(@TIdHashCRC16.HashValue, 'HashValue');
  end;
end;

(*----------------------------------------------------------------------------*)
procedure RIRegister_IdHashCRC(CL: TPSRuntimeClassImporter);
begin
  RIRegister_TIdHashCRC16(CL);
  RIRegister_TIdHashCRC32(CL);
end;



{ TPSImport_IdHashCRC }
(*----------------------------------------------------------------------------*)
procedure TPSImport_IdHashCRC.CompileImport1(CompExec: TPSScript);
begin
  SIRegister_IdHashCRC(CompExec.Comp);
end;
(*----------------------------------------------------------------------------*)
procedure TPSImport_IdHashCRC.ExecImport1(CompExec: TPSScript; const ri: TPSRuntimeClassImporter);
begin
  RIRegister_IdHashCRC(ri);
end;
(*----------------------------------------------------------------------------*)
 
 
end.
