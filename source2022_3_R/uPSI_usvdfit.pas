unit uPSI_usvdfit;
{
This file has been generated by UnitParser v0.7, written by M. Knight
and updated by NP. v/d Spek and George Birbilis. 
Source Code from Carlo Kok has been used to implement various sections of
UnitParser. Components of ROPS are used in the construction of UnitParser,
code implementing the class wrapper is taken from Carlo Kok's conv utility

}
interface
 

 
uses
   SysUtils
  ,Classes
  ,uPSComponent
  ,uPSRuntime
  ,uPSCompiler
  ;
 
type 
(*----------------------------------------------------------------------------*)
  TPSImport_usvdfit = class(TPSPlugin)
  public
    procedure CompileImport1(CompExec: TPSScript); override;
    procedure ExecImport1(CompExec: TPSScript; const ri: TPSRuntimeClassImporter); override;
  end;
 
 
{ compile-time registration functions }
procedure SIRegister_usvdfit(CL: TPSPascalCompiler);

{ run-time registration functions }
procedure RIRegister_usvdfit_Routines(S: TPSExec);

procedure Register;

implementation


uses
   utypes
  ,usvd
  ,usvdfit
  ;
 
 
procedure Register;
begin
  RegisterComponents('Pascal Script', [TPSImport_usvdfit]);
end;

(* === compile-time registration functions === *)
(*----------------------------------------------------------------------------*)
procedure SIRegister_usvdfit(CL: TPSPascalCompiler);
begin
 CL.AddDelphiFunction('Procedure SVDFit( X : TMatrix; Y : TVector; Lb, Ub, Nvar : Integer; ConsTerm : Boolean; SVDTol : Float; B : TVector; V : TMatrix)');
 CL.AddDelphiFunction('Procedure WSVDFit( X : TMatrix; Y, S : TVector; Lb, Ub, Nvar : Integer; ConsTerm : Boolean; SVDTol : Float; B : TVector; V : TMatrix)');
end;

(* === run-time registration functions === *)
(*----------------------------------------------------------------------------*)
procedure RIRegister_usvdfit_Routines(S: TPSExec);
begin
 S.RegisterDelphiFunction(@SVDFit, 'SVDFit', cdRegister);
 S.RegisterDelphiFunction(@WSVDFit, 'WSVDFit', cdRegister);
end;

 
 
{ TPSImport_usvdfit }
(*----------------------------------------------------------------------------*)
procedure TPSImport_usvdfit.CompileImport1(CompExec: TPSScript);
begin
  SIRegister_usvdfit(CompExec.Comp);
end;
(*----------------------------------------------------------------------------*)
procedure TPSImport_usvdfit.ExecImport1(CompExec: TPSScript; const ri: TPSRuntimeClassImporter);
begin
  RIRegister_usvdfit_Routines(CompExec.Exec); // comment it if no routines
end;
(*----------------------------------------------------------------------------*)
 
 
end.
